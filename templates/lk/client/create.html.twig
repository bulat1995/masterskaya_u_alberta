{% extends 'base.html.twig' %}
{% block body %}
<script type="text/javascript">
onload=function(){
    document.getElementById('client_bossName').addEventListener('keyup',setInface);
    document.getElementById('client_positionName').addEventListener('keyup',setInface);
}
function setInface(){
    document.getElementById('client_inFace').value=(
    document.getElementById('client_positionName').value+' '+document.getElementById('client_bossName').value);
}
</script>
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('client.list') }}">{{'clients'|trans}}</a></li>
        <li class="breadcrumb-item active">{{'client.new'|trans}}</li>
      </ol>
    </nav>
    <h2 class="mb-4">{{'client.new'|trans}} </h2>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-group">
                <button type="submit" class="btn btn-success pull-right">{{'add'|trans}}</button>
        </div>
    {{ form_end(form) }}
{% endblock %}