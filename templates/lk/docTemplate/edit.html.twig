{% extends 'base.html.twig' %}
{% block title %} {{'document.edit'|trans}} {% endblock %}
{% block body %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('template.list') }}">{{'templates'|trans}}</a></li>
        <li class="breadcrumb-item active">{{docTemplate.name}}</li>
        <li class="breadcrumb-item active">{{'edit'|trans}}</li>
      </ol>
    </nav>
    <h2 class="mb-4">{{'document.edit'|trans}} </h2>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-group">
                <button type="submit" class="btn btn-primary btn-outline pull-right">{{'edit'|trans}}</button>
        </div>
    {{ form_end(form) }}
{% endblock %}
