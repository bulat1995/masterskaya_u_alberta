{{ form_start(form) }}

    {{ form_widget(form) }}
      <div class="pull-right mt-2 mb-2">
        <h5><b>{{ 'document.price'|trans}}:</b>  <span id="fullPrice"></span>  {{ 'rubles.short'|trans}}</h5>
      </div>
      <div class="clearfix"></div>
        <div class="form-group">
                <button type="submit" class="btn btn-success btn-outline pull-right">{{'document.createButton'|trans}}</button>
        </div>
    {{ form_end(form) }}
