
{{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" class="btn btn-outline-success float-right" value="{{ 'edit'|trans }}" />
    </div>
{{ form_end(form) }}
